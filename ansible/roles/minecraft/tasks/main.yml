---
# The deployment, in the order the pieces depend on each other.

- name: 📦 Setup | Checkout and configuration
  ansible.builtin.include_tasks: checkout.yml

- name: 🚀 Deploy | Containers
  ansible.builtin.include_tasks: stack.yml

- name: ⚙️ Config | Once the server answers
  ansible.builtin.include_tasks: post.yml
