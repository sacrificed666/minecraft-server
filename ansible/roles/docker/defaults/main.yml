---
docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin

# Container logs are capped per service in compose.yaml; this covers anything
# started outside it.
docker_log_max_size: 20m
docker_log_max_file: "5"
